package com.ecommerce.pages;

import java.util.List;
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.StaleElementReferenceException;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class CheckoutPage {
	
	private WebDriver driver;
	
	public CheckoutPage(WebDriver driver) {
		this.driver=driver;
	}
	
	public void updateItemQuantity(String quantity) throws InterruptedException{
		
		List<WebElement> price = driver.findElements(By.className("pricedisplay"));
//		System.out.println("Number of pricedisplay classes are: "+price.size());
		System.out.println("Current price of an item: "+price.get(0).getText());
	
		WebElement updateQuantity = driver.findElement(By.name("quantity"));
		updateQuantity.clear();
		updateQuantity.sendKeys(quantity);
		List<WebElement> submit = driver.findElements(By.name("submit"));
//		System.out.println("Number of submit buttons are: "+submit.size());
		submit.get(0).click();
//		updateQuantity.submit();
//		driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);
		Thread.sleep(4000);
		List<WebElement> price2 = driver.findElements(By.className("pricedisplay"));
		System.out.println("Updated price of an item: "+price2.get(0).getText());
		
		List<WebElement> removeButton = driver.findElements(By.name("submit"));
		removeButton.get(1).click();
		Thread.sleep(2000);
		
		if(removeButton.isEmpty())
			System.out.println("Remove item failed");
		else
			System.out.println("Remove item is successful");
		
	}

}
